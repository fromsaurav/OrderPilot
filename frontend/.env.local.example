# Copy to .env.local. Points the UI at the API (local docker-compose by default; in the cloud
# deploy set this to the EC2 public IP NodePort, e.g. http://<ec2-ip>:30080).
NEXT_PUBLIC_API_BASE=http://localhost:8000
